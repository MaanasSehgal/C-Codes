// WAP to find out sum of series up to n terms.

#include <stdio.h>
#include <math.h>

int main()
{
    int x, n, sum = 0, nFact = 1;
    printf("Enter the value of x and n");
    scanf("%d %d", &x, &n);

for(int i = 0; i <= n; i+=2) {
    
}

    for (int i = 1; i <= n; i++)
    {
        nFact *= i;
    }
}